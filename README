计算机网络课设，要求：
基本任务（达标任务）
   完成两台主机之间的数据通信（数据链路层）
	仿真ARP协议获得网段内主机的MAC表
	使用帧完成两台主机的通信（Hello! I’m …)        
高端任务（优秀任务）
    完成两台主机通过中间主机的数据通信（网络层）
 	增加基于IP地址的转发功能
 	增加网络层封装

高端任务解释：
	A-->B-->C
	A知道B的MAC和C的IP，然后信息发给B，B从自己的表里面找C的信息，然后发给他。
	A的工作：发送；
	B的工作：接收，发送
	C的工作：接收
	方便起见，A和C均不再写接收模块，B接收到消息以后也不再向A发送消息。